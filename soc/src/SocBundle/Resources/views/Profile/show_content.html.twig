{% trans_default_domain 'FOSUserBundle' %}

Test

<div class="fos_user_user_show jumbotron">
    <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
    <p>{{ 'First name'|trans }}: {{ user.firstname }}</p>
    <p>{{ 'Last name'|trans }}: {{ user.lastname }}</p>
    <p>{{ 'Address'|trans }}: {{ user.address }}</p>
    <a href="{{ path('fos_user_profile_edit') }}"><button type="btn btn-default" class="btn btn-primary" name="button">Edit</button></a>
</div>
